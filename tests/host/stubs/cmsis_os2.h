/* Host-test stub: opaque CMSIS-RTOS message-queue id for can_globals.h.
 * The queue handles it declares are never referenced by the FSM/EBS code
 * under test. */
#pragma once
typedef void* osMessageQueueId_t;
